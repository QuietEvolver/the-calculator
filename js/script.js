const inputtedName = prompt("Please enter your name:")
let greeting = alert("Hi " + inputtedName + ", have a great day!");

const prompt("First number:" + parseInt(number1));
const prompt("Second number:" + parseInt(number2));
const subtraction = alert("Please two numbers to subtract:")
let subtractionResult = alert("Hi inputtedName, the difference is " + subtractionResult(number1 - number2) + ", have a great day!");

const multiplication = alert("Please two numbers to multiply:")
let multiplicationResult = alert("Hi inputtedName, the difference is " + subtractionResult(number1 * number2) + ", have a great day!");

const number3;
const threeTimes = parseInt(number1 * number2 * number3);
